# The dependency list lives in pyproject.toml, which is the single source of
# truth. This file installs from it, so `pip install -r requirements.txt` and
# `pip install .` cannot drift apart.
#
# For the test, type-check and lint tooling as well:
#     pip install -e '.[dev]'
.
